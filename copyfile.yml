- name: Copy Files over SSH
  hosts: all
  tasks:
          - name: start copy
            copy:
                    src: /home/ansible/ansible-tower/inventory
                    dest: /home/ansible/Desktop

